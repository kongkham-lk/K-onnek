# Kongkham Luangkhot Contact Card

A lightweight, configuration-driven digital contact card for Kongkham Luangkhot.

## Development

```bash
npm install
npm run dev
```

Update personal details in `src/config/siteConfig.js`. Build the static site with `npm run build`.
